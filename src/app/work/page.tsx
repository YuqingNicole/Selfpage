import { Work } from "@/views/Work";

export const metadata = {
  title: "Work — Nicole",
  description: "Projects, experience, and things I've built.",
};

export default function WorkPage() {
  return <Work />;
}
